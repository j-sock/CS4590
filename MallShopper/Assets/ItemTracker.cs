using UnityEngine;
using System.Collections;

public class ItemTracker : MonoBehaviour {
	/*
	private Transform current;
	//public AudioClip ambient;
	
	void Start () {
		 audio.clip = ambient;
		 audio.volume = 0;
		 audio.loop = true;
		 audio.Play();
	}	
	
	void Update () {
			if(current != null) {
			float distance = Vector3.Distance(transform.position, current.position);
			float vertDistance = current.position.y - transform.position.y;
		 	audio.pitch = vertDistance / 20 + 1;
			// audio.volume = 1f - Mathf.Log10(distance) / 2;
		 }
	}
	
	public void startTracking(Transform item) {
		current = item;
		audio.volume = .5f;
	}
	
	public void mute() {
		audio.volume = 0;
	} */
}
